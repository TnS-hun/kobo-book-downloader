# User and Group IDs for file permissions
# On Unraid, these are typically 99:100 (nobody:users)
PUID=99
PGID=100

# Timezone for proper log timestamps
# Find your timezone at: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TZ=America/New_York

# Web UI settings
# Enable the web UI (true/false). When false, the container runs the CLI only.
UI_ENABLED=true
# Port exposed by the container for the web UI
UI_PORT=5000

# Paths (relative to docker-compose.yml location)
# These will be mounted as volumes in the container
CONFIG_PATH=./config
DOWNLOADS_PATH=./downloads

# Optional: Custom container name
CONTAINER_NAME=kobo-book-downloader

# Optional: Custom image tag
IMAGE_TAG=latest

# CLI command to run when UI_ENABLED=false (arguments to kobo-book-downloader)
# Examples:
#   KBD_COMMAND="list --all"
#   KBD_COMMAND="pick /downloads"
#   KBD_COMMAND="get /downloads --all"
KBD_COMMAND="list --all"
